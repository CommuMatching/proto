//
//  Setprofileview.swift
//  proto
//
//  Created by 玉川悠真 on 2023/05/09.
//

import SwiftUI

struct Setprofileview: View {
    
    var body: some View {
        Text("")
        /*Menu {
            Button("アップロード", action: {
                self.source = .photoLibrary
                self.isImagePicker.toggle()
                viewModel.imageData = imageData
            })
            Button("写真を撮る", action: {
                self.source = .camera
                self.isImagePicker.toggle()
                viewModel.imageData = imageData
            })
        } label: {
        }*/
    }
}

struct Setprofileview_Previews: PreviewProvider {
    static var previews: some View {
        Setprofileview()
    }
}
